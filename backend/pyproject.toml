[project]
name = "loanify-backend"
version = "0.1.0"
description = "Loan approval prediction API"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "pandas",
    "joblib",
    "scikit-learn",
    "shap",
]

[tool.fastapi-cli]
app = "main:app"